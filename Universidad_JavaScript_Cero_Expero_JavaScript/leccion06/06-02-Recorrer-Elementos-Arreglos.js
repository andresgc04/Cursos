const autos = ["BMW", "Mercedes Benz", "Volvo"];

//Recorrer elementos de un arreglo por su indice:
console.log(autos[0]);
console.log(autos[2]);

for (let i = 0; i < autos.length; i++) {
  console.log(autos[i]);
}
